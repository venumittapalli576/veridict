# Veridict verifies itself. This is the config its own CI uses.
commands:
  tests: "pytest -q"
  build: null
  lint: "ruff check ."
  typecheck: null

checks:
  no_secrets: true
  no_new_todos: true

required:
  - tests

timeout: 300
